{{#bs-progress-bar animate=random.pollingEnabled
                   striped=true
                   showLabel=true
                   type=(if complete 'success' 'default')
                   value=percent
                   minValue=0
                   maxValue=100 as |percent|}}
  {{#if complete}}
    {{completeText}}
  {{else}}
    Initializing… {{percent}}
  {{/if}}
{{/bs-progress-bar}}
