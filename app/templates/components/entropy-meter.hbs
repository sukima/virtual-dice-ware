{{#bs-progress as |p|}}
  {{#p.bar
      animate=isPolling
      striped=true
      showLabel=true
      type=meterType
      value=percent
      minValue=0
      maxValue=100 as |percent|}}
    {{#if isPolling}}
      {{#if complete}}
        Harvesting Entropy
      {{else}}
        Initializing… {{percent}}
      {{/if}}
    {{else}}
      Paused Entropy Harvesting
    {{/if}}
  {{/p.bar}}
{{/bs-progress}}
